void checkSphere(int r, int p, int q) {
    double diagonal = sqrt(p * p + q * q) / 2;
    if (r <= diagonal) {
        cout << "Пройдет" << endl;
    } else {
        cout << "Не пройдет" << endl;
    }
}
